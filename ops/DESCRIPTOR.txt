v3.79
[FEAT] Overhauled Console Panel UI and logging architecture. Removed filter sliding tray and pill overlays in favor of clean, inline text filters using standard theme colors (Pastel Blue, Red, Orange, Green). Implemented "ALL DEVICES" vs specific Active Device IP filtering. Fixed duplicate SYSTEM logs caused by React StrictMode WebSocket echo loops. Added ASCII art startup logo and updated export format to ATEM_WEB_MANAGER_%DATE%_%TIME%.txt. Wired comprehensive [SYSTEM] logging across all App.jsx settings changes and Mixer panel resets. Set default quadrant mapping to STREAM, MEDIA POOL, MIXER, and CONSOLE.