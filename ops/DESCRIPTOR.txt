v3.78
[FEAT] Overhauled Console Panel logging architecture. Introduced discrete log source tagging (USER, ATEM, BRIDGE, SYSTEM) with muted typography and distinct color mapping. Built a smooth sliding inline filter tray into the console header, tying directly into the EXPORT generator. Upgraded bridge/server.js to intelligently filter out ATEM UDP noise, logging only actionable hardware state changes and tracking explicit user UI dispatches. Set default Quadrant routing to STREAM, MEDIA POOL, MIXER, and MACROS.